# ============================================================================
# Sherpa MCP Server Configuration
# ============================================================================

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
SERVER_BASE_URL=http://localhost:8000

# ============================================================================
# Auth0 OAuth Configuration
# ============================================================================
# To enable Auth0 authentication, uncomment and fill in the following variables.
# See AUTH0_SETUP.md for detailed setup instructions.

# Auth0 Domain - Replace YOUR_DOMAIN with your Auth0 domain
# AUTH0_CONFIG_URL=https://YOUR_DOMAIN.auth0.com/.well-known/openid-configuration

# Auth0 Application Credentials
# Get these from your Auth0 Application settings
# AUTH0_CLIENT_ID=your-client-id-here
# AUTH0_CLIENT_SECRET=your-client-secret-here

# Auth0 API Audience
# This is the identifier you set when creating an API in Auth0
# AUTH0_AUDIENCE=https://api.your-app.com

# OAuth Settings (optional)
# REQUIRE_CONSENT=true  # Show consent screen (recommended for production)

# ============================================================================
# Advanced OAuth Configuration (Optional)
# ============================================================================
# These are handled automatically by default, but can be customized if needed

# JWT Signing Key (optional - will be derived from client_secret if not set)
# FASTMCP_SERVER_AUTH_AUTH0_JWT_SIGNING_KEY=your-secret-signing-key

# Required Scopes (optional - defaults to "openid")
# FASTMCP_SERVER_AUTH_AUTH0_REQUIRED_SCOPES=openid,profile,email

# Allowed Client Redirect URIs (optional - for MCP clients)
# FASTMCP_SERVER_AUTH_AUTH0_ALLOWED_CLIENT_REDIRECT_URIS=http://localhost:*

# ============================================================================
# Google Calendar Configuration
# ============================================================================
# To enable Google Calendar integration:
# 1. Create a Google Cloud project at https://console.cloud.google.com
# 2. Enable the Google Calendar API
# 3. Create OAuth 2.0 credentials (Desktop application type)
# 4. Download the credentials JSON and set GOOGLE_CALENDAR_CREDENTIALS_JSON
# 5. Run locally to authenticate - it will output GOOGLE_CALENDAR_TOKEN_JSON
#
# See GOOGLE_CALENDAR_SETUP.md for detailed instructions

# OAuth client credentials JSON (contents of downloaded credentials file)
# GOOGLE_CALENDAR_CREDENTIALS_JSON='{"installed":{"client_id":"...","client_secret":"..."}}'

# OAuth token JSON (generated after authentication - set this for production)
# GOOGLE_CALENDAR_TOKEN_JSON='{"token":"...","refresh_token":"...","client_id":"..."}'

# ============================================================================
# Future Integrations (Placeholders)
# ============================================================================

# TickTick
# TICKTICK_CLIENT_ID=
# TICKTICK_CLIENT_SECRET=

# Obsidian
# OBSIDIAN_VAULT_PATH=/path/to/vault

# Health Data
# HEALTH_DATA_API_KEY=
